#include <stdio.h>
/**
 * main -  prints the lowercase alphabet in reverse
 *
 * Return: always (0)
 */
int main(void)
{
	char t;

	t = 'z';
	while
		(t >= 'a')
		{
		putchar(t);
		t--;
		}
	putchar('\n');
	return (0);
}
